<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiangli.api.mapper.ShareSSRMapper">

    <select id="queryAll" resultType="com.jiangli.api.model.ShareSSR">
        SELECT * FROM SHARE_SSR WHERE IS_DELETED = 0 ORDER BY UPDATE_TIME DESC ;
    </select>

</mapper>